let accontState;
const accountType="Student";
let accountName= "Sangam"
var password= "12890"

accountNo= 12345 //in js variable can also be declared as this;

// variable assigned without any declaration keyword. In JavaScript, 
// this implicitly creates a global variable (not recommended)

console.table([accontState, accountName, accountType, password, accountNo]);

//never use var because of block scope and functional scope  problem
